
// Simple example test to show tests passing in jenkins pipeline
describe('instance', function () {
  it('should be defined', function () {
    var controller = {};
    expect(controller).toBeDefined();

  });
});